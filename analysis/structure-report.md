# InFocus — Repository Structure Report

## Summary
- Tech: React 19 + TypeScript 5, Vite 5, Tailwind, Supabase (Postgres + Auth), Capacitor optional, PWA plugin.
- Start commands: `npm run dev` (vite), `npm run build` (tsc && vite build), `npm run preview`.

## Key files inspected
- package.json: dev/start/build/preview and test:e2e scripts present.
- tsconfig.json: React JSX, strict mode, ESNext modules.
- vite.config.ts: Vite + React plugin, PWA config, env bindings for VITE_SUPABASE_* and API keys.
- src/main.tsx: App bootstrap — constructs MovieConductor with SupabaseMovieService and renders App.
- src/core/: contains Conductor implementation (MovieConductor.ts) and tests.
- src/components/: UI components exist (Toast, LoginScreen, BottomNav, etc.).
- src/services/: SupabaseMovieService, AuthService and mocks exist.
- src/lib/supabase.ts: singleton Supabase client created from import.meta.env.
- index.html: root entry exists and loads /src/main.tsx.

## Environment / Secrets
- Found `.env` at repository root containing VITE_SUPABASE_URL, VITE_SUPABASE_ANON_KEY, VITE_TMDB_API_KEY and VITE_OMDB_API_KEY.
- Risk: Secrets are present in repo workspace. DO NOT commit `.env` to version control. Create `.env.example` with placeholder values and remove any committed secrets.
- vite.config.ts exposes define bindings for process.env.* based on VITE_ prefixed envs.

## Tests & Lint
- package.json defines `test:e2e` (Playwright) but no top-level `test` or `lint` scripts.
- There are unit tests for MovieConductor and services, but TDD coverage unclear.

## Architecture notes (Conductor Pattern)
- Conductor pattern present under `src/core/conductor/MovieConductor.ts` (pure logic present).
- UI components are in `src/components/` and appear largely presentational.
- AuthService uses singleton Supabase client from `src/lib/supabase.ts` (good).

## Short checklist to reproduce locally
1. Copy `.env.example` → `.env` and fill keys (VITE_SUPABASE_URL, VITE_SUPABASE_ANON_KEY, VITE_TMDB_API_KEY, VITE_OMDB_API_KEY).
2. npm install
3. npm run dev

## Immediate risks & recommendations
1. Remove or rotate any secrets found in `.env` before committing or sharing repository snapshots.
2. Add `.env.example` to repo (placeholders only).
3. Add top-level `test` and `lint` npm scripts to make checks discoverable (`vitest` and ESLint/Prettier).
4. Verify RLS and `profiles` table migrations are documented and included in `supabase/migrations/`.

## Urgent tasks (prioritized)
1. Create `.env.example` (no secrets) and ensure `.gitignore` excludes `.env`.
2. Improve Conductor tests to follow TDD for critical flows (login, addMovie, updateWatchStatus) and add CI vitest job.
3. Add documentation for Supabase RLS & `profiles` schema/migrations (include SQL or migration path) so security guarantees are clear.

## Files to open next (suggested)
- src/core/config/AppConfig.ts
- src/lib/supabase.ts
- src/services/AuthService.ts
- src/services/SupabaseMovieService.ts
- src/core/conductor/MovieConductor.test.ts

---
Report generated by code analysis tool.